{
  "entities": {
    "TrendingProduct": {
      "title": "Trending Product",
      "description": "A product discovered as a trend, pending review or synced to Shopify.",
      "type": "object",
      "properties": {
        "title": { "type": "string", "description": "Raw product title" },
        "imageUrl": { "type": "string", "description": "Product image source" },
        "sourceCountry": { "type": "string", "description": "Country where the trend was found" },
        "trendScore": { "type": "number", "description": "Calculated score for the trending item" },
        "status": { 
          "type": "string", 
          "enum": ["pending_review", "approved", "synced_to_shopify", "campaign_live", "rejected"] 
        },
        "optimizedTitle": { "type": "string" },
        "optimizedDescription": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "shopifyUrl": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "ownerId": { "type": "string", "description": "UID of the user who owns this record" }
      },
      "required": ["title", "imageUrl", "sourceCountry", "trendScore", "status", "createdAt"]
    }
  },
  "firestore": {
    "/trending_products/{productId}": {
      "schema": "TrendingProduct",
      "description": "Collection of products being analyzed and marketed."
    }
  }
}
